<Window x:Class="Minesweeper.WPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Minesweeper.WPF"
        xmlns:viewmodels="clr-namespace:Minesweeper.WPF.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="1*" MaxHeight="65"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Game">
                <MenuItem Header="New game" Command="{Binding StartNewGameCommand}"/>
                <MenuItem Header="Restart" Command="{Binding RestartGameCommand}"/>
            </MenuItem>
        </Menu>
        <Canvas Grid.Row="1" Background="Black"/>
        <Canvas Grid.Row="2" x:Name="Container"/>
    </Grid>
</Window>
